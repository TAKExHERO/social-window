<div class='topPage'>
  <div class="container">
    <h1>社会の窓通報代理サービス</h1>
    <p>あなたの周りにいる男性の社会の窓が空いていたら気軽に「社会の窓空いているよ？」と言えるでしょうか？そんな方の代理で通報をいたします。</p>
    <%= form_with(model: @inquiry, url: inquiries_confirm_path, local: true) do |f| %>

      <% if @inquiry.errors.any? %>
        <div class="alert alert-danger" role="alert">
          <strong>入力内容にエラーがあります</strong>
          <ul>
            <% @inquiry.errors.each do |attr, msg| %>
              <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <div class="form-group">
       <%= f.label :name %>
        <%= f.text_field :name, autofocus: true, class: "form-control", placeholder: "お名前" %>
      </div>
      <div class="form-group">
       <%= f.label :email %>
        <%= f.text_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス" %>
      </div>
      <div class="form-group">
        <%= f.label :relationship %>
        <%= f.select(:relationship, [["同僚", "同僚"], ["先輩", "先輩"], ["後輩", "後輩"], ["同じ部署の者です", "同じ部署の者です"], ["違う部署の者です", "違う部署の者です"]], {}, class: "form-control") %>
      </div>
      <div class="form-group">
        <%= f.label :content %>
        <%= f.text_area :content, class: "form-control", placeholder: "合わせて伝えたいことがあれば入力してください" %>
      </div>
       <div class="text-center"><%= f.submit '確認画面へ', class: 'btn btn-primary' %></div>
    <% end %>
  </div>
</div>
